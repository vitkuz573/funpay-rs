// Mock tests require wiremock (not in dev-dependencies) and
// use modules not yet exported (search, retry).
// TODO: Enable when wiremock is added to [dev-dependencies]
// and search/retry modules are exported in lib.rs
